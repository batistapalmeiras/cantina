// jest-dom adds matchers for asserting on DOM nodes (e.g. toBeInTheDocument()).
// Under Vitest + jsdom on Node 20 the former CRA/jest polyfills (TextEncoder,
// crypto) are no longer needed — those globals are provided by the runtime.
// Libs
import '@testing-library/jest-dom';